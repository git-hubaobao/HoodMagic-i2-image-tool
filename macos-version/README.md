# HoodMagic i2 macOS ??

???????? macOS ????????? macOS ????????????????????????????????????? `apps/image-tool`????????????

## ?????

??????????HoodMagic i2 ?? macOS ?????

- ????? Electron ? `app.getPath('userData')` ?????????? Windows ?????
- ?????? Node `path` API ????????????? Windows ???
- ?????????? macOS ???????????????????Dock ???????????
- ?????????????????????? API ???????? Electron/?????????
- Windows ??? `rcedit.exe` ???????? `win32` ????????? macOS ???

???????macOS ?????????? macOS ????????????Windows ???? TypeScript/Electron ?????????????? macOS ????????

## ????

```text
macos-version/
  README.md                     # macOS ????
  electron-builder.macos.yml    # macOS ?? electron-builder ??
  assets/
    icon.icns                   # macOS App ??
  github-actions/
    build-macos.yml             # GitHub Actions macOS runner ??
  scripts/
    build-macos.sh              # macOS/Linux shell ????
    build-macos.ps1             # PowerShell ????????????
```

## ? macOS ???

? macOS ??????????????

```bash
chmod +x macos-version/scripts/build-macos.sh
macos-version/scripts/build-macos.sh
```

??????

```bash
pnpm install
pnpm --filter image-tool build
pnpm --filter image-tool exec electron-builder \
  --config ../../macos-version/electron-builder.macos.yml \
  --mac dmg zip \
  --universal \
  --publish never
```

???????

```text
macos-version/dist/
```

???????

```text
HoodMagic-i2-mac-universal-1.0.0.dmg
HoodMagic-i2-mac-universal-1.0.0.zip
```

## ?????

??????????????????????????? macOS ???????? Apple Developer ??????????????????????? Gatekeeper ???

????????? macOS ?????????

```bash
export CSC_IDENTITY_AUTO_DISCOVERY=true
export APPLE_ID="your-apple-id@example.com"
export APPLE_APP_SPECIFIC_PASSWORD="xxxx-xxxx-xxxx-xxxx"
export APPLE_TEAM_ID="YOURTEAMID"
```

?????????????????????????? Mac ????????????????

## Windows ????

????????? Windows???? Windows ????

- ???????
- TypeScript ??
- macOS ??????
- macOS ??????

???? Windows ??????? macOS `.dmg`?electron-builder ?????? Windows ??? macOS ?????? macOS ?????? macOS ????electron-builder ??????????????????????????????????macOS ????? macOS ????

## GitHub Actions ??

?????? Mac?????? GitHub Actions ? macOS runner ????????????

```text
macos-version/github-actions/build-macos.yml
```

GitHub ????????? `.github/workflows/` ?? workflow ?????????????????????????????????????? `.github/workflows/build-macos.yml`??????? Actions artifact ?? `macos-version/dist/` ??????

## ?????

? macOS ?????????

```bash
pnpm build
pnpm test
git status --short
```

?????????????????

- Apple Developer ????
- Notarization ??
- ? Intel Mac ? Apple Silicon Mac ?????????
- ???? SHA256 ???
